@model VisualizarDespesasViewModel

@{
    Layout = "_Layout";
    ViewBag.Titulo = "Visualização de Despesas";
}

<p>
    <a class="btn-amber px-4" href="/despesas/cadastrar">
        <i class="bi bi-plus"></i>
        Cadastrar Novo/a
    </a>
</p>

<hr />

<div class="d-flex flex-wrap gap-2">
    @foreach (var item in Model.Registros)
    {
        <div class="col-12 col-md-3">
            <div class="card border-0">
                <div class="card-body">
                    <h5 class="card-title">Descrição: #@item.Descricao</h5>
                    <p class="card-text"><span class="fw-semibold">Valor:</span> @item.Valor.ToString("C2")</p>

                    <span class="d-flex gap-1 justify-content-end mt-4">
                        <a href="/despesas/detalhes/@item.Id" class="btn btn-secondary opcao-card">
                            <i class="bi bi-search"></i>
                        </a>
                        <a href="/despesas/editar/@item.Id" class="btn-edit opcao-card">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a href="/despesas/excluir/@item.Id" class="btn-delete opcao-card">
                            <i class="bi bi-trash"></i>
                        </a>
                    </span>
                </div>
            </div>
        </div>
    }
</div>